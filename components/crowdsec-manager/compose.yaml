  crowdsec-manager:
    image: hhftechnology/crowdsec-manager:latest
    container_name: crowdsec-manager
    restart: unless-stopped
    expose:
      - "8080"
    environment:
      - PORT=8080
      - ENVIRONMENT=production
      - TRAEFIK_DYNAMIC_CONFIG=/app/config/traefik/dynamic_config.yml
      - TRAEFIK_STATIC_CONFIG=/app/config/traefik/traefik_config.yml
      - TRAEFIK_CONTAINER_NAME=${COMPOSE_PROJECT_NAME}-traefik-1
      - CROWDSEC_CONTAINER_NAME=${COMPOSE_PROJECT_NAME}-crowdsec-1
      - PANGOLIN_CONTAINER_NAME=${COMPOSE_PROJECT_NAME}-pangolin-1
      - GERBIL_CONTAINER_NAME=${COMPOSE_PROJECT_NAME}-gerbil-1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config:/app/config
      - ./compose.yaml:/app/docker-compose.yml
      - ./backups:/app/backups
      - ./data:/app/data
    labels:
      - "traefik.enable=false"
    networks:
      - default
