🤖 AgentGateway Platform Deployment

Deployment completed at: $(date)

📊 Configuration:
- Domain: ${DOMAIN}
- Admin Subdomain: ${ADMIN_SUBDOMAIN:-pangolin}
- OpenShell Controller Subdomain: ${OPENSHELL_CONTROLLER_SUBDOMAIN:-openshell-controller}
- Email: ${EMAIL}
- Admin Username: ${ADMIN_USERNAME}

🌐 Access Information:
- Admin Dashboard: https://${ADMIN_SUBDOMAIN:-pangolin}.${DOMAIN}
- MCP Auth (IDP): https://idp.${DOMAIN}
- OpenShell Controller: https://${OPENSHELL_CONTROLLER_SUBDOMAIN:-openshell-controller}.${DOMAIN}
- Traefik Dashboard: https://${TRAEFIK_SUBDOMAIN:-traefik}.${DOMAIN}/dashboard/
- Middleware Manager: https://${MIDDLEWARE_MANAGER_SUBDOMAIN:-middleware-manager}.${DOMAIN}

📁 Directory Structure Created:
./config/
├── config.yml               # Pangolin configuration
├── traefik/
│   ├── traefik_config.yml    # Traefik static configuration
│   └── rules/
│       └── dynamic_config.yml # Traefik dynamic routing
├── letsencrypt/              # SSL certificates
└── middleware-manager/       # Middleware configurations

🔧 Services Included:
- pangolin: Core application platform (admin dashboard + tunnel management)
- gerbil: WireGuard VPN management
- traefik: Reverse proxy with SSL termination
- pangolin-postgres: PostgreSQL database
- middleware-manager: Dynamic Traefik configuration
- crowdsec: Collaborative security engine
- mcpauth: OAuth/MCP authentication management
- openshell (native): OpenShell gateway (installed via install.sh, runs as systemd service)
- openshell-controller (native): OpenShell Controller dashboard on port 3000 (runs as systemd service)

🚀 Next Steps:
1. Ensure DNS records point all subdomains (including wildcard *.${DOMAIN}) to this server
2. Initialize the admin user:
   docker exec agentgateway-pangolin-1 pangctl set-admin-credentials --email "${ADMIN_USERNAME}" --password "${ADMIN_PASSWORD}"
3. Initialize the database:
   chmod +x initialize_sqlite.sh && ./initialize_sqlite.sh
4. Access your admin dashboard: https://${ADMIN_SUBDOMAIN:-pangolin}.${DOMAIN}
5. Access OpenShell Controller: https://${OPENSHELL_CONTROLLER_SUBDOMAIN:-openshell-controller}.${DOMAIN}

⚠️ Important Notes:
- SSL certificates will be automatically generated via Let's Encrypt
- Ensure ports 80, 443, and 51820 (UDP) are open and accessible
- OpenShell gateway and controller run as native systemd services (not in Docker)
- CrowdSec provides real-time threat protection

🔐 Security Features:
- CrowdSec collaborative threat protection
- SSL/TLS encryption for all services
- MCP OAuth authentication integration
- VPN access for secure remote management
- Role-based access control

🐚 OpenShell Features:
- Native OpenShell gateway (installed via NVIDIA install.sh)
- OpenShell Controller dashboard for managing sandboxed agent execution
- Accessible at https://${OPENSHELL_CONTROLLER_SUBDOMAIN:-openshell-controller}.${DOMAIN}
