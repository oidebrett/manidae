{
  "name": "static-page",
  "required_env": ["STATIC_PAGE_SUBDOMAIN"],
  "optional_env": ["DOMAIN", "ADMIN_SUBDOMAIN", "OPENAI_API_KEY", "KOMODO_HOST_IP"],
  "dependencies": ["middleware-manager"],
  "description": "Creates a static landing page and configures Traefik routing for it"
}
